[build]
  # Если фронтенд лежит в папке frontend — оставь эту строку,
  # если в корне репозитория, просто удали её.
  base = "frontend"

  # Папка, которую нужно задеплоить (после сборки).
  publish = "dist"

  # Команда сборки проекта.
  command = "npm run build"
